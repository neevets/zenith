test "advanced features parsing" {
    enum Status { Active, Inactive }
    struct User { 
        readonly $id: int; 
        $status: Status 
    }
    let $ok = true;
    z_assert($ok, "advanced syntax structures parsed successfully");
}
